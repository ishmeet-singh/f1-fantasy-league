name: ci

on:
  pull_request:
  push:
    branches: [main, master]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm
      - name: Diagnose npm network
        run: npm run diagnose:network
      - name: Install dependencies
        run: npm install
      - name: Unit tests
        run: npm test
      - name: Build
        run: npm run build
